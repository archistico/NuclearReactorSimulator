# M10.9.4.1 — Operational Envelope & Numerical Hardening

**Status:** IN PROGRESS — Phase G complete; validated through H.19; H.20 four-node activation/rollback/shadow-telemetry contract candidate prepared

**Current validated source checkpoint:** M10.9.4.1-H.19
**Working source checkpoint:** M10.9.4.1-H.20 candidate

## Validated cumulative scope

A–D close seed, drum, condenser, turbine-admission, governor, passive-loss and operator-valve hardening. E.2 establishes the 10 MWe bidirectional current-v2 reference plant; E.3.1/E.3.2 add measured evidence and supervised delayed electrical protection. F.1 validates the compressible steam-flow capacity seam. F.2 validates one conservative atmospheric header-relief boundary. F.3 Hotfix 1 validates the separate conservative internal header-to-condenser turbine bypass.

G.1 validates `h = u + p/rho` and quantifies the historical advection gap. G.2 Hotfix 2 validates passive pipe/valve enthalpy transport and exact pump work ownership. G.3 validates every remaining non-turbine current-v2 owner. G.4 validates turbine expansion: the supplied audit records 2.506379668 MW maximum flow work, 5.457103652 MW maximum shaft power and zero ownership residual, with shaft work single-owned, turbine work unretuned and legacy profiles preserved. **Phase G is complete.**

## H.1 — Fixed-Step Timestep Sensitivity & Stiffness Evidence — VALIDATED

The user-confirmed H.1 gate reports 10/5/2.5 ms cost of 0.824089/1.592215/3.188456 wall seconds per simulated second, large raw primary hydraulic one-step changes and non-improving maximum final relative difference: 0.005401937 coarse/medium versus 0.006028534 medium/fine. Conservation remains green.

## H.2 — Deterministic Semi-Implicit Pressure/Flow Method Decision — VALIDATED

H.2 selects deterministic semi-implicit pressure/flow coupling as the method direction. Production remains explicit at 10 ms and no semi-implicit runtime path is active in H.2. Bounded explicit substeps are not selected because refinement approximately doubles cost without monotonic convergence improvement.

See ADR 0126, `M10_9_4_1_H2_NUMERICAL_METHOD_DECISION.md` and `M10_9_4_1_H2_VALIDATION_CHECKLIST.md`.

## H.3 Hotfix 1 — Isolated Semi-Implicit Hydraulic Prototype & Audit — VALIDATED

- Reuses existing pipe/valve/pump laws in an audit-only bounded deterministic Picard iteration.
- Reconstructs frozen non-hydraulic forcing from the validated explicit current-v2 trajectory so the comparison changes only pressure/flow coupling.
- Records chatter, subcooled-liquid pressure change, convergence, ownership/conservation, deterministic repeat, final-state gap and cost.
- Remains preserved as the historical Picard implementation and comparison baseline.

## H.4 — Deterministic Hybrid Semi-Implicit Activation & Cost Gate — VALIDATED

H.4 selected `P060-F040-R015`: pressure trigger 0.060, flow trigger 40 kg/s, relaxation 0.15, 72 maximum corrector iterations. It corrected 2/50 intervals, converged 2/2, achieved deterministic work ratio 2.14 and observational cost ratio 1.662880, retained the required chatter/pressure improvements, zero conservation/ownership residuals and exact deterministic repeat. `activation-criteria-met=True`; H.4 itself remained production-explicit.

## H.5 Hotfix 1 — Current-v2 Hybrid Hydraulic Production Integration — REJECTED BY VALIDATION

Direct free-running production activation failed ordinary validation because the H.4 0.5 s frozen-forcing evidence did not cover the later difficult intervals.

## H.5 Hotfix 2 — Production Activation Rollback & Extended Shadow Qualification — VALIDATED

Restores both sustained current-v2 production profiles to `ExplicitCommittedState` at 10 ms. User validation passed build, ordinary tests and the focused gate. The extended 500-interval shadow audit triggered 7 corrections, converged 5/7, used deterministic work ratio 1.492000 and observational cost ratio 1.480162, preserved exact determinism/conservation and returned `extended-shadow-qualification-passes=False` without committing any shadow candidate.

## H.6 — Shadow Corrector Rescue Envelope & Two-Tier Qualification — VALIDATED

User validation passed compilation, ordinary tests and the focused audit. H.6 froze P060/F040 and the exact seven H.5 trigger events, then swept six bounded fixed-relaxation Picard profiles. `R0125-I096` was selected but converged only 6/7. The selected profile reported maximum pressure residual 0.291876228, maximum flow residual 61.700761261 kg/s and maximum explicit-end gaps mass/energy/pressure 0.000175566/0.000194823/0.291958117. The two-tier primary/rescue ladder also converged only 6/7 with deterministic work ratio 1.700000. `refined-envelope-qualification-passes=False`; production remained explicit and no shadow candidate was committed.

## H.7 Hotfix 1 — Corrector Algorithm Revision — VALIDATED

User validation passed build, ordinary tests and the focused audit. H.7 preserved the same 500 explicit intervals and seven frozen P060/F040 events, reproduced H.4 convergence 5/7 and H.6 rescue convergence 6/7, then evaluated a separate true-residual deterministic-backtracking corrector. H.7 converged 5/7 and exhausted its line search on two events. Maximum fixed-point residuals were pressure 0.303946536 and flow 28.424177648 kg/s; deterministic hydraulic-evaluation work ratio was 1.308000; accepted merit strictly decreased, exact repeat passed and conservation/ownership remained green. `corrector-algorithm-revision-qualification-passes=False`; production remained explicit. Hotfix 1 changed only the xUnit2031 test contract and is the validated H.7 baseline.

## H.8 — Accelerated Nonlinear Hydraulic Corrector — VALIDATED

User validation passed compilation, ordinary tests and the focused audit. H.8 reproduced H.4 5/7, H.6 6/7 and H.7 5/7, then safeguarded Anderson also converged 5/7 with two line-search exhaustions. It recorded Anderson attempts/acceptances 30/24, residual fallback 13/11, six rejected least-squares systems, maximum coefficient L1 norm 7.188310311, maximum pressure/flow residuals 0.303946566 / 28.444475059 kg/s, deterministic work ratio 1.212000, strict accepted-merit decrease and exact repeat. `accelerated-corrector-qualification-passes=False`; production remained explicit.

## H.9 — Jacobian-Informed Nonlinear Hydraulic Corrector — VALIDATED

User validation passed compilation, ordinary tests and the focused audit. H.9 reproduced H.4 5/7, H.6 6/7, H.7 5/7 and H.8 5/7, then finite-difference Jacobian / damped Newton also converged 5/7 with two line-search exhaustions. It built/accepted 23/21 Jacobian directions, rejected no Jacobian, used 736 probes at maximum dimension 32, reached maximum pivot-condition estimate 1.613327388 and maximum normalized Newton step 1.276749799. Maximum fixed-point pressure/flow residuals were 0.303946548 / 28.426478113 kg/s; deterministic work ratio was 2.702000; merit strictly decreased, exact repeat/conservation passed and `jacobian-informed-corrector-qualification-passes=False`. Production remained explicit.

## H.10 Hotfix 1 — Hydraulic Map Switching & Non-Smoothness Diagnosis — VALIDATED

User validation passed compilation, ordinary tests and focused audit. H.10 found zero hydraulic branch switches and zero hydraulic non-smooth paths around the two persistent H.9 candidates, with maximum hydraulic derivative-scale growth 1.000000000 and one-sided slope asymmetry 0.000054751. It found exactly two thermodynamic phase/envelope switches and two thermodynamic non-smooth nodes, with maximum thermodynamic derivative-scale growth 3.999974411; the matching explicit endpoints showed zero thermodynamic switches. Exact deterministic repeat passed. Hotfix 1 changed only the H.10 test compile/analyzer contract.

### H.11 — Thermodynamic Switching Localization & Active-Set Diagnosis — VALIDATED

H.11 consumes the validated H.10 evidence and probes only the two thermodynamic phase/envelope switching nodes. It localizes node identity, energy/mass crossing axis, resolved phase transition versus envelope edge, vapor quality and saturation-relative state, and reports node-local mapped-minus-applied hydraulic balance residuals. A suggested `hold-<nominal phase>` active set is diagnostic only; H.11 introduces no active-set or semi-smooth solver.

## Remaining phases

- validate H.20 build, ordinary suite and focused fail-closed activation/rollback contract audit;
- keep default current-v2 production explicit and keep H.20 supervisor unwired from `PlantNetworkOrchestrator`;
- if H.20 qualifies, prepare a separately reviewed opt-in production-integration candidate that retains immediate explicit rollback and full H.19 regression;
- if H.20 fails, repair the authority/rollback contract before any production wiring;
- I: compatibility and engineering hardening after Phase H closes;
- M10.9.5–M10.9.8 final operator-experience gates.


### H.12 — Thermodynamic Inverse Branch Selection Audit — VALIDATED

- Baseline: validated H.11.
- Purpose: explain the `steam` interval-200 and `stop-out` interval-360 phase jumps by observing all existing inverse-map roots and deterministic branch priority.
- Adds diagnostic-only branch candidate evidence; does not change `Resolve()` ordering or production results.
- Tests simultaneous saturated/superheated roots, coarse saturated detection toggles, late boundary-aware saturated-root shadowing and previous-state tie-break sensitivity.
- Production remains explicit at 10 ms; no active set, hysteresis, branch reorder or shadow commit.


### H.13 Hotfix 2 — Thermodynamic Branch Continuity / Hysteresis Shadow Experiment — VALIDATED

- Baseline: validated H.12.
- Scope: shadow-only and restricted to `steam` and `stop-out`.
- Compares previous-phase continuity with bounded previous-phase hysteresis (2% relative pressure / 5 K release limits).
- Uses the unchanged H.9 Jacobian corrector and the exact frozen seven P060/F040 events.
- Records 7/7 qualification, line-search exhaustion, deterministic work, branch overrides, target phase transitions, avoided branch jumps and conservation/ownership.
- Production `Resolve()`, hydraulic solvers, routing, trigger, coefficients and 10 ms timestep remain unchanged.
### H.14 — Broader Thermodynamic Branch-Continuity Shadow Qualification — CANDIDATE

- built only on validated H.13 Hotfix 2;
- retains production explicit 10 ms, production `Resolve()`, H.9 and the H.13 wrapper unchanged;
- extends the current-v2 shadow horizon from 500 to 2,000 committed intervals;
- preserves the first 500 intervals as the exact H.13 control window;
- evaluates every P060/F040 event in the extended horizon with unchanged H.9 plus targeted bounded 2%/5 K hysteresis;
- observes `steam`/`stop-out` branch decisions across all committed intervals;
- requires two deterministic near-boundary holds and two deterministic out-of-band releases covering both phase directions before broader qualification can pass;
- remains shadow-only and does not authorize activation.

### H.14 Hotfix 1 — Broader Thermodynamic Branch-Continuity Shadow Qualification — VALIDATED

User validation passed build, ordinary tests and focused audit. Over 2,000 committed intervals P060/F040 produced 15 events; unchanged H.9 plus targeted bounded hysteresis converged 14/15 and exhausted only interval 723. All four explicit hold/release challenges passed. The target-policy observation remained transparent on committed states, and interval 723 recorded zero branch overrides/releases. `broader-shadow-qualification-passes=False`; production remained explicit.

### H.15 Hotfix 1 — Extended Trigger 723 Root-Cause Diagnosis — VALIDATED

User validation passed build, ordinary suite and focused audit. H.15 found zero hydraulic switching/non-smooth paths and localized the interval-723 failure to `header`. The candidate `header` state contains overlapping saturated/superheated roots, misses the coarse saturated root, still exposes a valid boundary-aware saturated root and selects the earlier coarse-superheated branch. The explicit endpoint remains saturated. `header` is also the dominant local mass/energy residual owner. Hotfix 1 additionally forces stacked-ZIP validation to remove stale local `bin`/`obj` before build. Production remains explicit.

### H.16 — Extended Three-Node Branch-Continuity Shadow Qualification — CANDIDATE

H.16 preserves H.9, production `Resolve()`, P060/F040 and the validated 2%/5 K bounded hysteresis limits. Its audit first reproduces the H.14 two-node `steam|stop-out` control at 14/15 with interval 723 exhausted, then extends only the target set to `steam|stop-out|header` over the same 2,000 intervals and 15 triggers. Positive qualification requires 15/15, interval-723 recovery with concrete `header` override evidence, transparent 6,000 committed observations, inherited hold/release challenge success, exact repeat and preserved closure/ownership. No production activation is authorized.


### H.17 Hotfix 6 — Long-Horizon & Cross-Profile Branch-Continuity Diagnostic — VALIDATED

H.17 Hotfix 6 validated build, ordinary tests and the focused 30,000-interval/four-profile audit infrastructure. The P060/F040 census contains 3,046 trigger intervals grouped into 92 deterministic episodes and 473 qualified representatives. The H.16 control remains 15/15, but the three-node `steam|stop-out|header` policy converges only 228/473 representatives and exhausts 245/473. Committed target selection remains transparent and the inherited hold/release challenges pass. The all-node candidate/explicit scan discovers untargeted `turbine-inlet` branch disagreement: 120 failures have turbine-inlet phase mismatch, while 125 failures do not. Production remains explicit.

### H.18 Hotfix 1 — Turbine-Inlet Continuity Extension & Residual-Floor Split Diagnosis — VALIDATED

User validation passed compilation, the complete ordinary suite and the focused H.18 audit. H.18 evaluated all 245 H.17 failures plus 16 distributed success controls under unchanged H.9 and unchanged bounded 2%/5 K hysteresis extended only to `steam|stop-out|header|turbine-inlet`: 261/261 converged, all 120 mismatch failures and all 125 non-mismatch failures recovered, 16/16 controls were preserved, 14,746 turbine-inlet overrides were observed, committed selection remained transparent, exact repeat passed and no residual failure or new untargeted branch-disagreement node remained. `four-node-extension-qualifies=True`; production remained explicit.

### H.19 — Four-Node Long-Horizon & Cross-Profile Qualification — VALIDATED

User validation passed compilation, the complete ordinary suite and focused H.19 audit. The complete 30,000-interval/four-profile P060/F040 census reproduced 3,046 triggers, 92 episodes and the exact 473 frozen representative keys. The unchanged four-node policy converged 473/473 with zero line-search exhaustion, recovered 245/245 H.17 failures, preserved 228/228 H.17 successes, retained committed-selection transparency across 120,000 phase-state checks, passed deterministic/release/closure safeguards and exposed no new untargeted late-shadow or phase-mismatch node. Production remained explicit and no shadow state was committed.

### H.20 — Four-Node Activation Contract, Rollback & Shadow Telemetry — CANDIDATE

H.20 freezes the validated H.19 representative/metric evidence and introduces only a shadow fail-closed authority supervisor. The default activation arm is disabled. When armed inside the audit, a corrected candidate is eligible only if H.19 qualification evidence is accepted, convergence/line-search/residual/closure/ownership/branch guards are all green; otherwise authority immediately rolls back to `ExplicitCommittedState` with a typed reason. The supervisor cannot authorize production commit and is not wired into `PlantNetworkOrchestrator`. The focused gate covers all 473 H.19 representatives, eight rollback challenges, deterministic decision fingerprints and explicit production-default verification.
