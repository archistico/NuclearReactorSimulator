# M4.1 — Main Steam Network & Turbine Admission

## Status

**VALIDATED** — local build and complete automated test suite passed.

Built on validated M3.8 — Integrated Primary-Circuit Baseline.

## Delivered

- canonical `MainSteamNetworkDefinition` spanning every M3 steam-export seam;
- semantic `MainSteamLineDefinition` mapped to existing canonical plant pipes and header nodes;
- canonical `TurbineAdmissionTrainDefinition` with exact stop → control → admission valve-chain validation;
- replaceable `TurbineAdmissionBoundaryDefinition` at canonical turbine-inlet nodes;
- complete M4.1 inputs that require legacy M3 steam-export sinks to be zero while main-steam transport is active;
- committed-state per-line and per-valve hydraulic/energy diagnostics using validated M1 solvers;
- explicit temporary turbine-admission mass/energy sink using committed turbine-inlet specific internal energy;
- backward-compatible higher-phase source-term composition seam in `IntegratedPrimaryCircuitSolver`;
- exactly one conserved-inventory integration through the existing `PlantNetworkOrchestrator`;
- integrated M4.1 snapshot with turbine-inlet state, valve diagnostics, terminal flow/power and inherited global conservation audit;
- topology, legacy-boundary exclusion, fail-safe and integrated conservation tests;
- ADR 0032 and `docs/MAIN_STEAM_NETWORK.md`.

## Deliberately not included

- turbine expansion/stage groups;
- shaft work, torque, inertia or rotor speed;
- overspeed/trip behavior;
- condenser/vacuum/hotwell;
- condensate/feedwater train;
- generator/grid physics;
- automatic controls or protection.

## Next milestone

Local build and complete automated test-suite validation were reported successful. M4.1 is the validated baseline for M4.2.

## Acceptance

```powershell
dotnet restore
dotnet build --no-restore
dotnet test --no-build
```
