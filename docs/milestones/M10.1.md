# M10.1 — Operator Computer Contracts & Terminal Shell

## Goal

Establish the immutable Application-layer contracts and the first presentation-only fixed-page operator terminal shell without moving any physics, control, alarm, guidance, history or session ownership into App/UI.

## Status

**VALIDATED.**

The user explicitly confirmed M10.1 compiled, all tests passed and the terminal shell worked correctly. M10.1 is the validated baseline for M10.2. The final M9 user-supplied `MainWindow.axaml` remains the authoritative layout basis.

## Delivered scope

### Application contracts

- `OperatorComputerPageId` with the fixed eight pages:
  - GUIDANCE
  - INFO
  - ALARMS
  - COMMANDS
  - MODES
  - DIAGNOSTICS
  - LOG
  - SESSION
- immutable `OperatorComputerPageDescriptor`/`OperatorComputerPageSnapshot` contracts;
- immutable `OperatorComputerRuntimeStatusSnapshot`;
- immutable `OperatorComputerSnapshot` with exact fixed-page-set validation;
- `OperatorComputerSnapshotProjector` that projects only already-published `ControlRoomSnapshot` status into the terminal shell.

All M10.1 page content is explicitly `ShellOnly`. No future M10.2–M10.7 content is fabricated early.

### App terminal shell

- `OperatorComputerViewModel` owns only presentation selection/state;
- `ControlRoomComputerControl` provides a monospace HUD-style terminal shell;
- fixed top menu and fixed bottom status line;
- F1–F8 fixed page navigation;
- global MainWindow F1–F8 bindings open the Operator Computer workspace and select the requested page;
- `ControlRoomWorkspaceId.OperatorComputer` / catalog entry added as a seventh workspace;
- page selection remains App-only state and is preserved across runtime snapshot updates.

### User-validated MainWindow layout integration

The user's corrected `MainWindow.axaml` is the authoritative M10.1 layout basis. In particular it preserves:

- `MinWidth=1340`;
- fixed 240 / flexible center / 320 column layout;
- explicit side-panel backgrounds;
- center vertical scrolling with horizontal scrolling disabled;
- no synthetic `MinWidth=760` on center content;
- no forced left-alignment workaround on center scroll content;
- existing trailing vertical extent above the footer.

M10.1 adds only the terminal workspace/control, global F1–F8 bindings and M10.1 footer text on top of that user-validated layout.

## Ownership rules

M10.1 must not:

- introduce plant physics or calculations in App/Avalonia;
- own guidance/checklist rules;
- own alarm state or alarm-event history;
- add plant command/permissive logic;
- add training-guidance-mode mutations;
- add physical control-authority/automation semantics;
- add session/checkpoint/replay persistence;
- use wall-clock time as deterministic state.

Those capabilities remain staged for later M10 milestones and must reuse their canonical owners.

## Tests

M10.1 adds regressions for:

- exact eight-page catalog/order/labels;
- immutable snapshot page collection and fixed page-set validation;
- projection of logical step/run state/alarm/signal/protection status only from `ControlRoomSnapshot`;
- default GUIDANCE selection and all eight page commands;
- page-selection persistence across runtime snapshot updates;
- F1–F8 global bindings and dedicated terminal-control binding;
- fixed terminal menu/status line and absence of free-form text input;
- seventh Operator Computer workspace;
- preservation of the user-validated MainWindow center-layout contract.

## Deliberate non-goals

M10.1 does not yet implement actual guidance, plant info pages, alarm feed, contextual command catalog, control modes, diagnostics, log aggregation, session management or supervisory automation. Those belong to M10.2–M10.7.
