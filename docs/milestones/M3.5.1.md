# M3.5.1 — Main Circulation Test Namespace Hotfix

Status: **Baseline candidate**

## Purpose

Correct two test-only namespace imports introduced in M3.5.

`HeatDepositionFraction` is defined in:

`NuclearReactorSimulator.Domain.Physics.Reactor.ThermalPower`

The M3.5 circulation tests incorrectly imported the nonexistent namespace:

`NuclearReactorSimulator.Domain.Physics.Reactor.Core.ThermalPower`

## Changes

- Corrected the namespace import in `MainCirculationSystemDefinitionTests`.
- Corrected the namespace import in `MainCirculationSystemSolverTests`.
- No production code changed.
- No API, equation, topology rule, hydraulic behavior, or simulation behavior changed.

## Validation

Run:

```powershell
dotnet build --no-restore
dotnet test --no-build
```
